# Backend test dependencies
# Install with: pip install -r tests/requirements-test.txt

pytest>=8.0.0
pytest-cov>=5.0.0       # coverage reporting
pytest-mock>=3.14.0     # mocker fixture (optional, we use unittest.mock directly)
pytest-flask>=1.3.0     # Flask-aware fixtures

# The main requirements.txt must also be installed:
# pip install -r requirements.txt
